{
  "name": "deutsche-grammatik",
  "private": true,
  "description": "German grammar reference — static site; the package is only used for the tests.",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
